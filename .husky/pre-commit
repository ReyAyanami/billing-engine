echo "🔍 Running pre-commit checks..."

# Run lint-staged (lints and formats staged files)
echo "📝 Linting and formatting staged files..."
npx lint-staged || {
  echo "❌ Linting failed. Please fix the errors and try again."
  exit 1
}

# Run TypeScript type checking
echo "🔧 Running TypeScript type check..."
npm run type-check || {
  echo "❌ Type check failed. Please fix the TypeScript errors and try again."
  exit 1
}

echo "✅ All pre-commit checks passed!"
